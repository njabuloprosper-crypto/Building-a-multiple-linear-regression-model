# Building-a-multiple-linear-regression-model
This Python script builds a multiple linear regression model. It  fits the regression model on the training data, and generates predictions for the test set. Finally, it compares predicted vs. actual values, calculates prediction errors, and evaluates model performance to assess how well the model generalizes.
